/**
 * Re-export shim — the dialog moved to task-details/ in the tabbed restructure
 * (Översikt / Ekonomi / Checklistor / Foton / Anteckningar / Relaterat).
 * Kept so the existing import path works for all call sites.
 */
export { TaskEditDialog } from "./task-details/TaskEditDialog";
